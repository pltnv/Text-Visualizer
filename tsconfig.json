{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "allowSyntheticDefaultImports": true,
    "baseUrl": ".", // Указывает корневую директорию для разрешения не-относительных модулей
    "paths": {
      "@/*": ["./src/*"] // Соответствует алиасу, заданному в Vite
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.d.ts",
    "src/**/*.tsx",
    "src/**/*.vue" // Убедитесь, что TypeScript включает Vue-файлы
  ]
}
